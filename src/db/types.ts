export interface User {
    email: string;
    password: string;
}

export interface Entry {
    id: string;
    createdDate: Date;
    content: string;
}

export interface Newsletter {
    id: string;
    createdDate: Date;
    content: string;
}

